
# Configuração para suportar módulos TypeScript/JSX diretamente no navegador
<IfModule mod_mime.c>
  AddType application/javascript .ts
  AddType application/javascript .tsx
</IfModule>

# Garante que o navegador não cacheie versões erradas durante seus testes
<IfModule mod_headers.c>
  <FilesMatch "\.(ts|tsx|js|html)$">
    Header set Cache-Control "max-age=0, no-cache, no-store, must-revalidate"
    Header set Pragma "no-cache"
    Header set Expires "0"
  </FilesMatch>
</IfModule>
